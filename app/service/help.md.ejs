<!-- deno-fmt-ignore-file -->
# Help Menu

## About

`@Bott` (they/them) is a helpful agent that responds to your messages and generates media for you: photos, songs and videos.

## Commands

<% for (const action of Object.values(actions)) { %>
- `/<%= action.name %>`: <%= action.description %>
<% } %>

## Limitations

Bott may sometimes things that are not correct.

---

<% if (currentVersion) { %> <%= currentVersion %> . <% } %> written by DanielLaCos.se