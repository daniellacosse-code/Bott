// if (TEXT_GEN_FILE_PATH && Deno.statSync(TEXT_GEN_FILE_PATH).isDirectory) {
//   Deno.writeFileSync(join(TEXT_GEN_FILE_PATH, fileName), textData);
// }

export const writeFileSystem = (fileName: string, data: Uint8Array): URL => {};
